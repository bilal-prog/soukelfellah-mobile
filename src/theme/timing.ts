export const timing = {
  /**
   * The duration of a quick animation (in milliseconds).
   */
  quick: 150,
  /**
   * The duration of a standard animation (in milliseconds).
   */
  standard: 300,
} as const
